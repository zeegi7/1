def setup():
    size(200,200)
    noStroke()
    background("#050505")
def draw():
    fill(random(50,255))
    if mousePressed:
        pass
    else:
        circle(random(0,200),random(0,200),10)
        
